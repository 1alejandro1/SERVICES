@using BCP.CROSS.MODELS
@using BCP.CROSS.MODELS.Caso
@model ServiceResponse<List<CasosByEstadoResponse>>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@{
    ViewData["Title"] = "Supervisor/Bandeja de Entrada";
}

<div style="min-width:720px;">
   
    @if (Model?.Meta?.StatusCode == 200 && Model?.Data?.Count > 0)
    {
        <div class="row">
            <div class="col-12">
                <div class="card bg-bgcontent-dark rounded shadow mt-3 p-3">
                    <table id="tblBandejaAnalista" class="table table-sm text-light mb-2 mt-3 rounded">
                        <thead>
                            <tr class=text-opacity-75>
                                <th scope="col">Iniciar</th>
                                <th scope="col">Nro Caso</th>
                                <th scope="col">Idc Cliente</th>
                                <th scope="col">Empresa</th>
                                <th scope="col">Servicio</th>
                                <th scope="col">Estado</th>
                                <th scope="col">Detalle</th>
                                <th scope="col">Atendido por</th>
                                <th scope="col">Funcionario</th>
                                <th scope="col">Dias Atención</th>
                                <th scope="col">Dias Retraso</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Data)
                            {
                                <tr>
                                    <td>
                                        <a href="@Url.Action("SolucionCaso", "Supervisor", new{nroCaso = @item.NroCaso, matricula = @item.FuncionarioAtencion, funcionario = @item.FuncionarioNombre})">  <i class="fas fa-edit text-success"></i></a>
                                    </td>
                                    <td>@item.NroCaso</td>
                                    <td>@item.IdcCliente</td>
                                    <td>@item.NombreEmpresa</td>
                                    <td>@item.Descripcion</td>
                                    <td>@item.Estado</td>
                                    <td>@item.InformacionAdicional</td>
                                    <td>@item.FuncionarioAtencion</td>
                                    <td>@item.FuncionarioNombre</td>
                                    <td>@item.DiasAtencion</td>
                                    <td>@item.DiasDiferencia</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else if (@Model != null && @Model?.Meta?.StatusCode != 200)
    {
        <div class="row">
            <div class="col">
                <div class="alert alert-danger">@Model?.Meta?.Msj</div>
            </div>
        </div>
    }
    else{
         <div class="row">
            <div class="col">
                <div class="alert alert-info">No hay datos que mostrar</div>
            </div>
        </div>
    }
</div>


@section Scripts {
<script src="~/js/datatables.min.js"></script>
<script>
    $(document).ready(function () {
        $('#tblBandejaAnalista').dataTable({
            "info": false,
            "lengthMenu": [10, 25, 50],
            "language": {
                "search": "Filtrar:",
                "lengthMenu": "Mostar _MENU_",
                "paginate": {
                    "first": "Primero",
                    "last": "Último",
                    "next": "Siguiente",
                    "previous": "Atras"
                }
            }
        });
    });
</script>
}


